// import { useEffect, useState } from "react"
// just for testing

const Test = () => {
    let chars = [ 'A', 'B', 'A', 'C', 'B' ]

    let Res = arr.filter((x, index) => {
        return arr.indexOf(x) !== index
    })

    return <>{Res}</>
}

export default Test
