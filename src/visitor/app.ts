export { }
/*
!navigator.cookieEnabled
    ? window.open("../visitor/error.php?r=cc", "_self")
    : ""

let fx = setInterval(() => {
    if (typeof func === "function") {
        clearInterval(fx)

        const ggl = "https://www.google.com"

        gck("rd") ? window.open(ggl, "_self") : "" //~ check if redir
        gck('vic') ? import('../js/vic.js') : '' //~ visitor check

        if (gck("vic")) {

            import('../js/uag.js') //~ check user agent

            //~ visitor check cookies
            gck("vic") === "e"
                ? window.open("../visitor/error.php?r=cv", "_self")
                : ""

            gck("vic") === "r" ? window.open(ggl, "_self") : ""

            if (gck("vic") === "O") {
                let stateCheck = setInterval(() => {
                    if (document.readyState === "complete") {
                        clearInterval(stateCheck)
                        // do stuff..
                    }
                }, 1000)
            }
        }
    }
}, 400)
*/
