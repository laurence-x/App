export { }
/*
const ggl = "https://www.google.com"
const pth = window.location.pathname

let req = new XMLHttpRequest()
r_uag.open("GET", "../visitor/vic.php")
r_uag.onload = () => {
    if (r_uag.status === 200) {
        let res = r_uag.responseText
        console.log(res)
        res.trim() === "O" ? window.open(pth, "_self") : ""
        res.trim() === "r" ? window.open(ggl, "_self") : ""
        res.trim() === "e"
            ? window.open("../visitor/error.php?r=ce", "_self")
            : ""
    } else {
        window.open("../visitor/error.php?r=sc", "_self")
    }
}
r_uag.onerror = () => {
    window.open("../visitor/error.php?r=rc", "_self")
}
r_uag.send()
*/
